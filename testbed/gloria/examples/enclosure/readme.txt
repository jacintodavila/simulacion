consult('enclosure.main').

make_module( enclosure0, 'enclosure.kb' ).

prolog_agent(enclosure0, 0, 400,[]).

enclosure0:actionsmem(enclosure0, T, A, P).

listing(enclosure0:_). 

prolog_agent(enclosure0, 0, 400,[]).
